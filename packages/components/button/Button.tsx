import React from 'react';

const Button = () => {
    return (
        <button style={{backgroundColor:'blue',color:'red'}}>
            this is a button
        </button>
    );
}

export default Button;
